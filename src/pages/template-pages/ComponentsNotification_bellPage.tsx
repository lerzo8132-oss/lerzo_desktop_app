import TemplateHtmlPage from '../../components/TemplateHtmlPage';

const html = "<!-- Notification Bell with Dropdown -->\n<div class=\"relative\" id=\"notification-container\">\n    <button id=\"notification-bell\" class=\"relative p-2 text-gray-600 hover:text-gray-900 transition-colors\" \n            onclick=\"toggleNotificationDropdown()\" title=\"Notifications\">\n        <i data-lucide=\"bell\" class=\"w-6 h-6\"></i>\n        <!-- Badge for unread count -->\n        <span id=\"unread-badge\" class=\"absolute -top-1 -right-1 bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center hidden\">\n            <span id=\"unread-count\">0</span>\n        </span>\n    </button>\n\n    <!-- Dropdown Menu -->\n    <div id=\"notification-dropdown\" class=\"hidden absolute right-0 mt-2 w-96 bg-white rounded-2xl shadow-lg border border-gray-200 z-50 max-h-96 flex flex-col\">\n        <!-- Header -->\n        <div class=\"flex items-center justify-between px-4 py-3 border-b border-gray-100\">\n            <h3 class=\"font-bold text-gray-900\">Notifications</h3>\n            <button onclick=\"markAllNotificationsAsRead()\" class=\"text-xs text-accent hover:text-accent-hover font-semibold\">\n                Mark all read\n            </button>\n        </div>\n\n        <!-- Notifications List -->\n        <div id=\"notifications-list\" class=\"overflow-y-auto flex-1 divide-y divide-gray-100\">\n            <!-- Loading state -->\n            <div class=\"px-4 py-6 text-center text-gray-500\">\n                <i data-lucide=\"loader\" class=\"w-5 h-5 animate-spin mx-auto mb-2\"></i>\n                <p>Loading notifications...</p>\n            </div>\n        </div>\n\n        <!-- Footer -->\n        <div class=\"px-4 py-3 border-t border-gray-100\">\n            <a href=\"#notifications\" onclick=\"viewAllNotifications()\" class=\"text-center block text-sm text-accent hover:text-accent-hover font-semibold transition-colors\">\n                View All Notifications\n            </a>\n        </div>\n    </div>\n</div>\n\n<!-- Notification Item Template (used dynamically) -->\n<template id=\"notification-item-template\">\n    <div class=\"notification-item px-4 py-3 hover:bg-gray-50 transition-colors cursor-pointer\" \n         onclick=\"markNotificationAsRead(this.dataset.id)\">\n        <div class=\"flex items-start gap-3\">\n            <div class=\"flex-shrink-0\">\n                <div class=\"w-2 h-2 rounded-full bg-accent mt-2 unread-indicator\"></div>\n            </div>\n            <div class=\"flex-1 min-w-0\">\n                <h4 class=\"font-semibold text-gray-900 text-sm truncate notification-title\">Notification</h4>\n                <p class=\"text-gray-600 text-xs mt-1 line-clamp-2 notification-message\">Message</p>\n                <p class=\"text-gray-400 text-xs mt-2 notification-time\">Just now</p>\n            </div>\n        </div>\n    </div>\n</template>\n\n<!-- Empty State Template -->\n<template id=\"empty-state-template\">\n    <div class=\"px-4 py-8 text-center\">\n        <i data-lucide=\"inbox\" class=\"w-10 h-10 text-gray-300 mx-auto mb-2\"></i>\n        <p class=\"text-gray-500 font-medium\">No notifications</p>\n    </div>\n</template>\n\n<script>\n// Notification system constants\nconst NOTIFICATION_POLL_INTERVAL = 30000; // 30 seconds\nconst NOTIFICATION_TIMESTAMP_FORMAT = 'relative'; // relative or absolute\nlet notificationPoller = null;\n\n/**\n * Initialize notification system\n */\nfunction initNotifications() {\n    if (typeof currentUser === 'undefined' || !currentUser.id) {\n        console.warn('[NOTIFY] Current user not defined, skipping initialization');\n        return;\n    }\n\n    console.log('[NOTIFY] Initializing notification system');\n    \n    // Initial load\n    loadNotifications();\n    \n    // Poll for new notifications\n    startNotificationPolling();\n    \n    // Close dropdown when clicking outside\n    document.addEventListener('click', (e) => {\n        if (!e.target.closest('#notification-container')) {\n            closeNotificationDropdown();\n        }\n    });\n\n    // Lucide icons need to be refreshed after template rendering\n    console.log('[NOTIFY] Notification system initialized');\n}\n\n/**\n * Load notifications from API\n */\nasync function loadNotifications() {\n    try {\n        const response = await fetch('/api/notifications/list?limit=10', {\n            method: 'GET',\n            credentials: 'include',\n            headers: {\n                'Content-Type': 'application/json'\n            }\n        });\n\n        if (!response.ok) {\n            if (response.status === 401) {\n                console.warn('[NOTIFY] Unauthorized - user may have logged out');\n                return;\n            }\n            throw new Error(`HTTP error! status: ${response.status}`);\n        }\n\n        const data = await response.json();\n        \n        if (data.success && data.notifications) {\n            renderNotifications(data.notifications);\n            updateUnreadCount(data.notifications);\n        }\n    } catch (error) {\n        console.error('[NOTIFY] Error loading notifications:', error);\n    }\n}\n\n/**\n * Render notifications in dropdown\n */\nfunction renderNotifications(notifications) {\n    const listContainer = document.getElementById('notifications-list');\n    \n    if (!listContainer) return;\n\n    // Clear existing notifications\n    listContainer.innerHTML = '';\n\n    if (notifications.length === 0) {\n        // Show empty state\n        const emptyTemplate = document.getElementById('empty-state-template');\n        if (emptyTemplate) {\n            listContainer.appendChild(emptyTemplate.content.cloneNode(true));\n        }\n        return;\n    }\n\n    // Add each notification\n    const template = document.getElementById('notification-item-template');\n    if (!template) {\n        console.error('[NOTIFY] Notification template not found');\n        return;\n    }\n\n    notifications.forEach(notif => {\n        const clone = template.content.cloneNode(true);\n        \n        // Set data attributes\n        const item = clone.querySelector('.notification-item');\n        item.dataset.id = notif.id;\n        item.classList.toggle('unread', !notif.is_read);\n        \n        // Set content\n        clone.querySelector('.notification-title').textContent = notif.title;\n        clone.querySelector('.notification-message').textContent = notif.message;\n            clone.querySelector('.notification-staff').innerHTML = `Staff: <span class=\"font-semibold\">${notif.staff_name || 'Unknown'}</span>`;\n    });\n\n    // Refresh Lucide icons\n    if (typeof lucide !== 'undefined') {\n        lucide.createIcons();\n    }\n}\n\n/**\n * Update unread badge count\n */\nfunction updateUnreadCount(notifications) {\n    const unreadCount = notifications.filter(n => !n.is_read).length;\n    const badge = document.getElementById('unread-badge');\n    const countSpan = document.getElementById('unread-count');\n\n    if (countSpan) {\n        countSpan.textContent = unreadCount;\n    }\n\n    if (badge) {\n        if (unreadCount > 0) {\n            badge.classList.remove('hidden');\n        } else {\n            badge.classList.add('hidden');\n        }\n    }\n}\n\n/**\n * Format notification timestamp\n */\nfunction formatNotificationTime(isoString) {\n    try {\n        const date = new Date(isoString);\n        const now = new Date();\n        const diffMs = now - date;\n        const diffMins = Math.floor(diffMs / 60000);\n        const diffHours = Math.floor(diffMs / 3600000);\n        const diffDays = Math.floor(diffMs / 86400000);\n\n        if (diffMins < 1) return 'Just now';\n        if (diffMins < 60) return `${diffMins}m ago`;\n        if (diffHours < 24) return `${diffHours}h ago`;\n        if (diffDays < 7) return `${diffDays}d ago`;\n        \n        return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });\n    } catch (e) {\n        console.error('[NOTIFY] Error formatting time:', e);\n        return 'Unknown';\n    }\n}\n\n/**\n * Toggle notification dropdown\n */\nfunction toggleNotificationDropdown() {\n    const dropdown = document.getElementById('notification-dropdown');\n    if (dropdown) {\n        dropdown.classList.toggle('hidden');\n        if (!dropdown.classList.contains('hidden')) {\n            // Reload when opening\n            loadNotifications();\n        }\n    }\n}\n\n/**\n * Close notification dropdown\n */\nfunction closeNotificationDropdown() {\n    const dropdown = document.getElementById('notification-dropdown');\n    if (dropdown) {\n        dropdown.classList.add('hidden');\n    }\n}\n\n/**\n * Mark single notification as read\n */\nasync function markNotificationAsRead(notificationId) {\n    try {\n        const response = await fetch(`/api/notifications/${notificationId}/read`, {\n            method: 'POST',\n            credentials: 'include',\n            headers: {\n                'Content-Type': 'application/json'\n            }\n        });\n\n        if (response.ok) {\n            console.log(`[NOTIFY] Marked notification ${notificationId} as read`);\n            // Reload notifications\n            loadNotifications();\n        }\n    } catch (error) {\n        console.error('[NOTIFY] Error marking notification as read:', error);\n    }\n}\n\n/**\n * Mark all notifications as read\n */\nasync function markAllNotificationsAsRead() {\n    try {\n        const response = await fetch('/api/notifications/mark-all-read', {\n            method: 'POST',\n            credentials: 'include',\n            headers: {\n                'Content-Type': 'application/json'\n            }\n        });\n\n        if (response.ok) {\n            console.log('[NOTIFY] Marked all notifications as read');\n            loadNotifications();\n        }\n    } catch (error) {\n        console.error('[NOTIFY] Error marking all as read:', error);\n    }\n}\n\n/**\n * Start polling for new notifications\n */\nfunction startNotificationPolling() {\n    if (notificationPoller) {\n        clearInterval(notificationPoller);\n    }\n\n    notificationPoller = setInterval(() => {\n        loadNotifications();\n    }, NOTIFICATION_POLL_INTERVAL);\n\n    console.log(`[NOTIFY] Started polling every ${NOTIFICATION_POLL_INTERVAL}ms`);\n}\n\n/**\n * Stop polling for notifications\n */\nfunction stopNotificationPolling() {\n    if (notificationPoller) {\n        clearInterval(notificationPoller);\n        notificationPoller = null;\n        console.log('[NOTIFY] Stopped notification polling');\n    }\n}\n\n/**\n * View all notifications (navigate to notifications page)\n */\nfunction viewAllNotifications() {\n    if (typeof currentUser !== 'undefined' && currentUser.unique_id) {\n        window.location.href = `/c/${currentUser.unique_id}/notifications`;\n        return;\n    }\n    console.warn('[NOTIFY] Cannot navigate to notifications page. Missing user context.');\n}\n\n// Initialize when DOM is ready\ndocument.addEventListener('DOMContentLoaded', () => {\n    initNotifications();\n});\n\n// Clean up on page unload\nwindow.addEventListener('beforeunload', () => {\n    stopNotificationPolling();\n});\n</script>";

export default function ComponentsNotification_bellPage() {
  return <TemplateHtmlPage title="Components / Notification bell" templatePath="components/notification_bell.html" html={html} />;
}
